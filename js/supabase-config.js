/* ============================================================
   SUPABASE CONFIG
   Replace the two values below with your own project's info.
   Get them from: Supabase Dashboard → Project Settings → API
   → "Project URL" and "anon public" key.

   This file is meant to be public — the anon key is designed
   to be exposed in frontend code. Real security comes from the
   Row Level Security (RLS) policies you set on your tables and
   storage bucket, not from hiding these values.
   ============================================================ */
export const supabaseUrl = "https://cyewtjpztwlmmmmgrwlb.supabase.co";
export const supabaseAnonKey = "sb_publishable_g2wfeE8MiN79lJT4ZOgjHg_azT_hmNS";
